<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hungdo.team44phase3">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Signup"
            android:label="@string/title_activity_signup"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserScreen"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ProfileActivity"
            android:label="Profile" />
        <activity
            android:name=".AdminScreen"
            android:label="Admin" />
        <activity
            android:name=".EditProfileScreen"
            android:label="Edit Profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MyApplicationScreen"
            android:label="My application" />
        <activity android:name=".ViewAndApplyProjectScreen" />
        <activity android:name=".CourseInfoScreen" />
        <activity
            android:name=".AdminViewApp"
            android:label="@string/title_activity_admin_view_app"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdminPopProject"
            android:label="@string/title_activity_admin_pop_project"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>